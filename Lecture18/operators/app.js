let ans=10;
let naam; // automatic to undefined and undefined =false 
// if have given anything then output =true
if(ans>=5 && naam){
    console.log("hii");
}

//  agar me naam nhi define karta and if me ans>=5 deta but && ki jagah || deta toh output print hota because it will not check further condition if it get true first.